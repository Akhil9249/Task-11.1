const add = (first,second)=>{
    return first+second
}

const sub = (first,second)=>{
    return first-second
}

const divide = (first,second)=>{
    return first/second
}

module.exports = {add,sub,divide}

